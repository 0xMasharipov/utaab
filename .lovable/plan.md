

# Reorder Anar After Umut

Move Anar's entry from the end of the `teamMembers` array to directly after Umut in both files.

## Changes

### `src/components/Team.tsx` (lines 20-29)
```
zinurbek → yunus → abdulla → abdulbaki → umut → anar → yana → shuayb → ibrahim → burak
```

### `src/pages/TeamPage.tsx` (lines 28-37)
Same reorder — move `anar` from position 10 to position 6 (after umut).


Remove the "Built across leading blockchain networks" ecosystem section from UBpointPage.

Changes:
- Delete the `ecosystemCoins` array (lines 725-730).
- Delete the `EcosystemStrip` component definition (lines 732-765).
- Remove the `<EcosystemStrip />` render call from the page JSX (line 1015).
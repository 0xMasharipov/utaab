

# Remove Stats Section

Remove the `<Stats />` component from the homepage.

### Change
**File:** `src/pages/Index.tsx`
- Remove the `<Stats />` import and its usage from the JSX (between `<HeroCarousel />` and `<Community />`)

